jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Access organization secret
        env:
          API_KEY: ${{ secrets.API_KEY }}
        run: echo "Using shared organization API key"
